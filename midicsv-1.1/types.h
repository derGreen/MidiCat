/*

    	Type definitions for MIDI tools
	
*/

typedef unsigned char byte; 	    /* MIDI data stream byte */
typedef unsigned short vlint;	    /* Variable length integer: this must
    	    	    	    	       be an unsigned type of at least
				       32 bits (shorter is OK).  */
